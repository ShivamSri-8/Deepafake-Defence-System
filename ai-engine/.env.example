# EDDS AI Engine Environment Configuration

# API Settings
DEBUG=True
HOST=0.0.0.0
PORT=8000

# CORS Origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# Model Paths (relative to ai-engine directory)
# MODELS_DIR=models/weights
# XCEPTION_MODEL_PATH=models/weights/xception_deepfake.h5
# EFFICIENTNET_MODEL_PATH=models/weights/efficientnet_deepfake.h5
# LSTM_MODEL_PATH=models/weights/cnn_lstm_deepfake.h5

# Upload Settings
# MAX_FILE_SIZE=104857600  # 100MB in bytes

# Detection Thresholds
FAKE_THRESHOLD=0.5
HIGH_CONFIDENCE_THRESHOLD=0.85
LOW_CONFIDENCE_THRESHOLD=0.35

# Ensemble Weights
XCEPTION_WEIGHT=0.35
EFFICIENTNET_WEIGHT=0.40
LSTM_WEIGHT=0.25
